export class Engine {
    constructor(
        public name: string,
        public repo: string,
    ){}
}

//Engine blueprint class that contains the properties of what will be contained
// in the form for searching up user names and specific repositories